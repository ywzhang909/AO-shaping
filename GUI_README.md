# AO系统仿真GUI使用指南

基于PySide6开发的AO系统仿真控制台，提供完整的自适应光学系统仿真和可视化功能。

## 功能特性

### 配置面板
- **网格参数**: 设置仿真网格大小(N)、物理孔径(L)、波长
- **大气参数**: 配置湍流强度(Cn²)、外尺度(L₀)、内尺度(l₀)
- **变形镜参数**: 设置致动器数量、行程、插值选项
- **波前传感器参数**: 配置子孔径数量、像素比例
- **传播参数**: 设置传播距离

### 控制面板
- **DM电压控制**: 为每个致动器提供滑块和数值输入控件
- **批量操作**: 全部置零、随机化电压
- **仿真控制**: 重置系统、单步执行

### 结果显示面板
- **相机图像**: 显示探测到的光强分布，支持缩放和颜色映射调整
- **波前可视化**: 显示斜率向量场、X/Y分量或幅度
- **性能指标**: 实时显示Strehl比、总功率、RMS、PV值

### 菜单功能
- **文件菜单**: 加载/保存配置为JSON格式
- **视图菜单**: 预留扩展
- **帮助菜单**: 关于对话框

## 安装依赖

使用uv安装（推荐）:

```bash
uv add PySide6
```

或使用pip:

```bash
pip install PySide6 matplotlib numpy
```

## 运行GUI

```bash
python scripts/run_gui.py
```

## 使用流程

1. **配置系统**: 在左侧配置面板中设置AO系统参数
2. **应用配置**: 点击"应用配置"按钮初始化系统
3. **调整DM电压**: 使用控制面板中的滑块手动调整变形镜电压
4. **观察结果**: 在右侧面板查看相机图像和波前斜率
5. **性能监控**: 底部状态栏显示Strehl比，指标面板显示详细性能数据

## 配置保存/加载

- **保存配置**: 文件 -> 保存配置，选择JSON文件保存当前配置
- **加载配置**: 文件 -> 加载配置，从JSON文件恢复配置

## 技术实现

- **多线程架构**: 使用QThread处理仿真计算，避免GUI冻结
- **实时更新**: 电压变化立即触发仿真更新
- **模块化设计**: 各组件独立，便于维护和扩展
- **matplotlib集成**: 高质量的科学可视化

## 扩展性

GUI设计支持未来扩展：
- 添加自动控制算法
- 实现时间序列仿真
- 支持多种波前重建方法
- 添加数据导出功能

## 故障排除

- 确保所有依赖已正确安装
- 检查Python版本兼容性(PySide6要求Python 3.8+)
- 如遇matplotlib后端问题，尝试设置不同的后端

## 开发说明

GUI代码位于 `src/ao_shaping/gui/` 目录：
- `main_window.py`: 主窗口和应用逻辑
- `panels/`: 各功能面板
- `workers/`: 后台工作线程

核心仿真逻辑在 `src/ao_shaping/sim/devices.py` 的 `TraditionalAOSystem` 类中。</content>
</xai:function_call">创建GUI使用指南。现在总结完成的工作。