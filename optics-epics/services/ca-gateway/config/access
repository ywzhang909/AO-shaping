# =============================================================================
# ca-gateway/config/access - CA Gateway 访问安全规则
#
# 格式:EPICS access security 语法。
# ASG(组名) { RULE(序号, 权限) 规则体 }
#   READ  允许读取
#   WRITE 允许写入
#   TRAPWRITE 记录写入(配合 -putlog)
#
# 本文件为实验室默认:允许所有已白名单 PV 的读/写。
# 如需限制写入,可增加 HOST 子句,例如:
#   RULE(1, READ)
#   RULE(2, WRITE) HOST { 192.168.1.* }
# =============================================================================
ASG(DEFAULT) {
    RULE(1, READ)
    RULE(2, WRITE)
}
